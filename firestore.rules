rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // WIDE OPEN for development - secure later
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}