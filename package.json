{
  "name": "employee-evaluation-system",
  "version": "1.0.0",
  "description": "Multi-tenant employee evaluation and performance management system with Firebase backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run functions:serve\" \"npm run client:dev\"",
    "functions:serve": "cd functions && npm run serve",
    "functions:deploy": "firebase deploy --only functions",
    "client:dev": "cd frontend && npm start",
    "client:build": "cd frontend && npm run build",
    "install:all": "npm install && cd functions && npm install && cd ../frontend && npm install",
    "build": "npm run client:build && cd functions && npm run build",
    "deploy": "firebase deploy",
    "emulator": "firebase emulators:start",
    "start": "firebase serve"
  },
  "keywords": ["employee-evaluation", "performance-management", "multi-tenant", "firebase"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "firebase-tools": "^12.9.1"
  }
}
